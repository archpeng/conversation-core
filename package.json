{
  "name": "pms-agent-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run && pnpm guard:boundaries && pnpm eval",
    "eval": "tsc -b packages/evals && node packages/evals/dist/index.js",
    "guard:boundaries": "node scripts/boundary-guard.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
